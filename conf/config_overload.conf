# --- Simulazione ---
SIM_DURATION         = 2
N_NANO_SECS          = 1000000    # 1 ms reale = 1 min simulato

# --- Risorse umane ---
NOF_WORKERS          = 4          # 1 operatore per stazione
NOF_USERS            = 40         # Numero realistico di utenti per evitare process storm

# --- Postazioni per stazione ---
NOF_WK_SEATS_PRIMI   = 2
NOF_WK_SEATS_SECONDI = 2
NOF_WK_SEATS_COFFEE  = 2
NOF_WK_SEATS_CASSA   = 2

# --- Posti tavolo ---
NOF_TABLE_SEATS      = 20

# --- Tempi medi di servizio (in secondi simulati) ---
AVG_SRVC_PRIMI       = 500       # Servizio molto lento (15 min) per creare intenzionalmente la coda
AVG_SRVC_SECONDI     = 500       # Servizio moderato (5 min)
AVG_SRVC_COFFEE      = 30         # Servizio veloce (30 sec)
AVG_SRVC_CASSA       = 60         # Servizio veloce (1 min)

# --- Porzioni ---
AVG_REFILL_PRIMI     = 20
AVG_REFILL_SECONDI   = 5
MAX_PORZIONI_PRIMI   = 40
MAX_PORZIONI_SECONDI = 15

# --- Pause operatore ---
NOF_PAUSE            = 15       # Nessuna pausa per comportamento deterministico

# --- Prezzi ---
PRICE_PRIMI          = 4.50
PRICE_SECONDI        = 5.50
PRICE_COFFEE         = 1.20

# --- Terminazione ---
OVERLOAD_THRESHOLD   = 5          # Se a fine giornata ci sono più di 5 persone in coda scatta overload

# --- Menu ---
MENU_FILE            = conf/menu.txt

